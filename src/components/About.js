import { exo2 } from "@/styles/utils/fonts.js";

const About = () => {
  return (
    <div className="container-about" style={exo2.style}>
      <h2>About Me</h2>

      <p>
        Expat, French, mum, carriere switcher, those are a few of my facettes,
        Well there is a lot to tell, so why not just me around a cup of coffee ?
      </p>
    </div>
  );
};

export default About;
